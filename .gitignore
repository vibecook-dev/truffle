node_modules/
dist/
*.tsbuildinfo

# NAPI native binaries
*.node

# NAPI generated loader — regenerated by `napi build` at every build/publish
# and embeds the package version (was drifting on version bumps). index.d.ts
# stays committed (types, consumed by tsc); index.js is generated output.
crates/truffle-napi/index.js

# Rust build output
target/

# Local env overrides (test/bench auth keys, etc.)
.env
.env.local

# Benchmark output
target/bench-results/
bench-results/

# Sidecar binaries
packages/sidecar-slim/bin/
packages/sidecar-slim/sidecar-slim
packages/sidecar-slim/sidecar-slim-linux
packages/core/bin/

# Test sidecar binary (built from Go source)
crates/truffle-core/tests/test-sidecar

# VitePress
docs/.vitepress/dist/
docs/.vitepress/cache/

# State directories
.truffle-state/
apple/.vendor/

# SwiftPM build products for the root manifest (sources live in apple/,
# but `swift build` at the repo root writes here).
/.build/
/.swiftpm/
/Package.resolved

# OS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
sidecar-slim-linux
test-sidecar
/sidecar-slim
